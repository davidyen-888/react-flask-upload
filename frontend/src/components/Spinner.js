// This is a spinner component that can be used to show loading state in the app.

import React from "react";
import "./Spinner.css";

const Spinner = () => {
  return <div className="spinner"></div>;
};

export default Spinner;
